import { OpenApiExplorer } from '@/components/openapi/openapi-explorer';

export const metadata = { title: 'CareerPilot API Reference' };

export default function ApiDocsPage() {
  return <OpenApiExplorer />;
}
